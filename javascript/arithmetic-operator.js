//operator 

//arithmetic operator 
//+ → Addition (do numbers ko jodta hai)
// - → Subtraction (ek number me se dusra ghataata hai)
// * → Multiplication (do numbers ko guna karta hai)
//  / → Division (pehle number ko doosre se divide karta hai)
// % → Modulus (division ka remainder deta hai)
// ** → Exponentiation (power nikalta hai, jaise 2**3 = 8)
// ++ → Increment (value ko 1 badhata hai)
// -- → Decrement (value ko 1 ghatata hai)


//1. assignment operator (value assign karne ke liye)
// = → Value assign karta hai (x = 5)
// += → Pehle add karke assign karta hai (x += 2 → x = x+2)
// -= → Pehle subtract karke assign karta hai
// *= → Pehle multiply karke assign karta hai
// /= → Pehle divide karke assign karta hai
// %= → Pehle remainder nikal kar assign karta hai

var z = 10  // = jo voh assignmnet operator
console.log("z", z);

//2. aditional operator

var num1 = 20;
var num2 = 30;
var total = num1 + num2;
console.log("total", total);

//3. multiplication operator 
var aa = 9;
var bb = 7;
var multi = aa*bb;
console.log("multiplication result", multi);

//4. division operator 
var aa = 88;   // u can also use no1 for aa like var no1 = 88;
var bb = 4;
var divi = aa/bb;
console.log("division result", divi);

//5.  % modulus operator 
var no1 = 6;
var no2 = 5;
var mod = no1 % no2;
console.log("modulus result", mod); // modulus return the value of remainder after division

//6. increment operator
var rr = 11;
rr++;  // checking differnt possibilities
//rr = rr+1;
//rr = rr+"hello tani";
//rr +=1;
console.log("rr",rr);

//7.   decrement operator 
var ss = 66;
ss--;         // checking differnt possibilities
//ss = ss-1;
//ss -=1;
console.log("ss",ss);